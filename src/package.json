{
  "name": "sepine-image-map",
  "version": "1.0.0",
  "description": "Image map project with local server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:css": "tailwindcss -i tailwind.css -o style.css",
    "watch:css": "tailwindcss -i tailwind.css -o style.css --watch"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.3.5"
  }
} 